name = "kv_store"
exec = "/bin/kv_store"
args = []
restart = "always"
seccomp_profile = "net-service"
memory_limit = "16M"
cpu_quota = "25%"
after = ["httpd"]

[env]
PATH = "/bin:/usr/bin:/sbin"
LOG_LEVEL = "info"
