# Karim MicroVM OS — Layer Specification Config
# Declare additional OCI package layers to merge into the hermetic rootfs image.

# Example OCI layer declaration:
# [[layer]]
# name = "nginx-standalone"
# type = "oci-archive"
# path = "/var/lib/karim/layers/nginx_alpine.tar"
# target_dir = "/"
